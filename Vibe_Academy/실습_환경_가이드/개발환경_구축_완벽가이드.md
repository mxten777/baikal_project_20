# 🛠 Vibe Academy 실습 환경 설정 완벽 가이드

**Windows, macOS, Linux 환경별 완전 설치 매뉴얼**

---

## 📋 목차

1. [기본 도구 설치](#1-기본-도구-설치)
2. [개발 환경 구축](#2-개발-환경-구축)
3. [Firebase 프로젝트 설정](#3-firebase-프로젝트-설정)
4. [Vercel 배포 환경](#4-vercel-배포-환경)
5. [AI 도구 설정](#5-ai-도구-설정)
6. [문제 해결 가이드](#6-문제-해결-가이드)

---

## 📦 1. 기본 도구 설치

### 🖥 시스템 요구사항

#### 최소 요구사항
- **RAM**: 8GB 이상
- **저장공간**: 10GB 이상 여유 공간
- **인터넷**: 안정적인 브로드밴드 연결
- **운영체제**: Windows 10/11, macOS 10.15+, Ubuntu 18.04+

#### 권장 요구사항
- **RAM**: 16GB 이상
- **저장공간**: 20GB 이상 SSD
- **CPU**: Intel i5/AMD Ryzen 5 이상
- **모니터**: 1920x1080 이상 (듀얼 모니터 권장)

### 🌐 웹 브라우저

#### Chrome (권장)
```bash
# Windows
https://www.google.com/chrome/ 다운로드 후 설치

# macOS  
brew install --cask google-chrome

# Ubuntu
wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
sudo apt update
sudo apt install google-chrome-stable
```

#### 필수 Chrome 확장프로그램
- **React Developer Tools**
- **Redux DevTools** (필요시)
- **JSON Viewer**
- **Wappalyzer** (기술 스택 분석)
- **ColorZilla** (색상 추출)

### 📝 텍스트 에디터/IDE

#### Visual Studio Code (필수)

**Windows 설치**:
```powershell
# Chocolatey 사용
choco install vscode

# 또는 직접 다운로드
# https://code.visualstudio.com/download
```

**macOS 설치**:
```bash
# Homebrew 사용
brew install --cask visual-studio-code

# 또는 직접 다운로드
# https://code.visualstudio.com/download
```

**Linux 설치**:
```bash
# Ubuntu/Debian
wget -qO- https://packages.microsoft.com/keys/microsoft.asc | gpg --dearmor > packages.microsoft.gpg
sudo install -o root -g root -m 644 packages.microsoft.gpg /etc/apt/trusted.gpg.d/
sudo sh -c 'echo "deb [arch=amd64,arm64,armhf signed-by=/etc/apt/trusted.gpg.d/packages.microsoft.gpg] https://packages.microsoft.com/repos/code stable main" > /etc/apt/sources.list.d/vscode.list'
sudo apt update
sudo apt install code
```

#### 필수 VS Code 확장프로그램

**설치 방법**: `Ctrl+Shift+X` (확장프로그램 탭) → 검색 → 설치

```json
{
  "recommendations": [
    "esbenp.prettier-vscode",
    "bradlc.vscode-tailwindcss", 
    "ms-vscode.vscode-typescript-next",
    "formulahendry.auto-rename-tag",
    "christian-kohler.path-intellisense",
    "ms-vscode.vscode-json",
    "ms-vsliveshare.vsliveshare",
    "github.copilot",
    "github.copilot-chat",
    "ms-vscode.js-debug-nightly",
    "firefox-devtools.vscode-firefox-debug",
    "ms-vscode.live-server"
  ]
}
```

**설치 스크립트** (터미널에서 실행):
```bash
code --install-extension esbenp.prettier-vscode
code --install-extension bradlc.vscode-tailwindcss
code --install-extension ms-vscode.vscode-typescript-next
code --install-extension formulahendry.auto-rename-tag
code --install-extension christian-kohler.path-intellisense
code --install-extension ms-vscode.vscode-json
code --install-extension ms-vsliveshare.vsliveshare
code --install-extension github.copilot
code --install-extension github.copilot-chat
```

#### VS Code 설정 (settings.json)

`Ctrl+Shift+P` → "Preferences: Open Settings (JSON)" 선택 후 다음 설정 적용:

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "editor.tabSize": 2,
  "editor.insertSpaces": true,
  "editor.wordWrap": "on",
  "editor.minimap.enabled": true,
  "editor.fontSize": 14,
  "editor.fontFamily": "'Fira Code', 'Cascadia Code', Consolas, monospace",
  "editor.fontLigatures": true,
  "terminal.integrated.fontSize": 13,
  "workbench.colorTheme": "Default Dark+",
  "workbench.iconTheme": "vs-seti",
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000,
  "emmet.includeLanguages": {
    "javascript": "javascriptreact"
  },
  "tailwindCSS.includeLanguages": {
    "javascript": "javascript",
    "html": "HTML"
  },
  "prettier.semi": true,
  "prettier.singleQuote": true,
  "prettier.tabWidth": 2,
  "prettier.trailingComma": "es5"
}
```

---

## 🚀 2. 개발 환경 구축

### 📱 Node.js 설치

#### NVM을 통한 설치 (권장)

**Windows (nvm-windows)**:
```powershell
# 1. https://github.com/coreybutler/nvm-windows/releases 에서 최신 버전 다운로드
# 2. nvm-setup.zip 다운로드 후 설치
# 3. 새 터미널 열고 확인
nvm version

# Node.js LTS 설치
nvm install 18.18.0
nvm use 18.18.0
```

**macOS/Linux**:
```bash
# nvm 설치
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 터미널 재시작 또는 소스 새로고침
source ~/.bashrc
# 또는
source ~/.zshrc

# Node.js LTS 설치
nvm install 18.18.0
nvm use 18.18.0
nvm alias default 18.18.0
```

#### 직접 설치
```bash
# Windows/macOS/Linux
# https://nodejs.org/en/download/ 에서 LTS 버전 다운로드

# 설치 확인
node --version  # v18.18.0
npm --version   # 9.8.1
```

### 🔧 추가 패키지 도구

#### Yarn (선택사항)
```bash
npm install -g yarn
yarn --version
```

#### pnpm (권장 - 빠른 패키지 매니저)
```bash
npm install -g pnpm
pnpm --version
```

### 🐙 Git 설치 및 설정

#### Git 설치

**Windows**:
```powershell
# Git for Windows 다운로드 및 설치
# https://git-scm.com/download/win

# 또는 Chocolatey
choco install git
```

**macOS**:
```bash
# Homebrew
brew install git

# 또는 Xcode Command Line Tools
xcode-select --install
```

**Linux**:
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install git

# CentOS/RHEL
sudo yum install git
```

#### Git 초기 설정
```bash
# 사용자 정보 설정 (GitHub과 동일하게)
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 기본 브랜치명 설정
git config --global init.defaultBranch main

# 편집기 설정 (VS Code)
git config --global core.editor "code --wait"

# 자동 CRLF 처리 (Windows)
git config --global core.autocrlf true

# 자동 CRLF 처리 (macOS/Linux)
git config --global core.autocrlf input

# 설정 확인
git config --list
```

### 🔑 SSH 키 설정

#### SSH 키 생성
```bash
# SSH 키 생성 (이메일은 GitHub 계정과 동일하게)
ssh-keygen -t ed25519 -C "your.email@example.com"

# 또는 RSA (구버전 호환용)
ssh-keygen -t rsa -b 4096 -C "your.email@example.com"

# SSH 에이전트 시작
eval "$(ssh-agent -s)"

# SSH 키 에이전트에 추가
ssh-add ~/.ssh/id_ed25519
# 또는
ssh-add ~/.ssh/id_rsa
```

#### 공개 키 복사
```bash
# macOS
pbcopy < ~/.ssh/id_ed25519.pub

# Linux
xclip -selection clipboard < ~/.ssh/id_ed25519.pub

# Windows (Git Bash)
cat ~/.ssh/id_ed25519.pub | clip

# 또는 파일 내용 직접 확인
cat ~/.ssh/id_ed25519.pub
```

#### GitHub에 SSH 키 등록
1. [GitHub Settings > SSH and GPG keys](https://github.com/settings/keys) 접속
2. "New SSH key" 클릭
3. Title: "Vibe Academy - [컴퓨터명]"
4. Key: 복사한 공개 키 붙여넣기
5. "Add SSH key" 클릭

#### SSH 연결 테스트
```bash
ssh -T git@github.com
# "Hi username! You've successfully authenticated..." 메시지 확인
```

---

## 🔥 3. Firebase 프로젝트 설정

### 🏗 Firebase 프로젝트 생성

#### 1. Firebase Console 접속
1. [Firebase Console](https://console.firebase.google.com/) 접속
2. Google 계정으로 로그인
3. "프로젝트 추가" 클릭

#### 2. 프로젝트 설정
```
프로젝트 이름: vibe-academy-[이름]-2024
프로젝트 ID: vibe-academy-[이름]-2024 (자동 생성)
Google Analytics: 사용 (권장)
Analytics 계정: 기본 계정 또는 새 계정 생성
```

#### 3. 웹 앱 등록
1. 프로젝트 대시보드에서 "웹" 아이콘 클릭
2. 앱 닉네임: "Vibe Academy Web App"
3. Firebase Hosting 설정: 체크 (나중에 사용)
4. "앱 등록" 클릭

#### 4. Firebase 설정 정보 저장
```javascript
// 제공되는 설정 정보 (나중에 사용)
const firebaseConfig = {
  apiKey: "your-api-key",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "your-app-id",
  measurementId: "G-XXXXXXXXXX"
};
```

### 🔐 Firebase 서비스 활성화

#### Authentication 설정
1. 왼쪽 메뉴 > "Authentication" 클릭
2. "시작하기" 버튼 클릭
3. "Sign-in method" 탭 선택
4. 사용할 로그인 방법 활성화:
   - **이메일/비밀번호**: 사용 설정
   - **Google**: 사용 설정 + 프로젝트 지원 이메일 입력

#### Firestore Database 설정
1. 왼쪽 메뉴 > "Firestore Database" 클릭
2. "데이터베이스 만들기" 클릭
3. 보안 규칙 모드 선택:
   - **프로덕션 모드**: 보안 강화 (권장)
   - **테스트 모드**: 개발용 (30일 제한)
4. 위치 선택: "asia-northeast3 (Seoul)" 선택

#### Storage 설정
1. 왼쪽 메뉴 > "Storage" 클릭
2. "시작하기" 버튼 클릭
3. 보안 규칙 모드 선택: "프로덕션 모드"
4. 위치: "asia-northeast3 (Seoul)"

### 🔧 Firebase CLI 설치

```bash
# npm을 통한 설치
npm install -g firebase-tools

# 설치 확인
firebase --version

# Firebase 로그인
firebase login

# 브라우저에서 로그인 완료 후 터미널 확인
firebase projects:list
```

### 📱 테스트 앱 생성

#### 기본 프로젝트 생성
```bash
# 새 폴더 생성
mkdir vibe-firebase-test
cd vibe-firebase-test

# Vite React 프로젝트 생성
npm create vite@latest . -- --template react
npm install

# Firebase SDK 설치
npm install firebase

# 추가 개발 도구
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

#### Firebase 설정 파일 생성
```javascript
// src/firebase.js
import { initializeApp } from 'firebase/app';
import { getAuth } from 'firebase/auth';
import { getFirestore } from 'firebase/firestore';
import { getStorage } from 'firebase/storage';

const firebaseConfig = {
  // Firebase Console에서 복사한 설정 정보
  apiKey: "your-api-key",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "your-app-id"
};

// Firebase 초기화
const app = initializeApp(firebaseConfig);

// 서비스 초기화
export const auth = getAuth(app);
export const db = getFirestore(app);
export const storage = getStorage(app);

export default app;
```

#### 연결 테스트
```jsx
// src/App.jsx
import { useState, useEffect } from 'react';
import { auth, db } from './firebase';
import { collection, addDoc, getDocs } from 'firebase/firestore';

function App() {
  const [connected, setConnected] = useState(false);

  useEffect(() => {
    // Firebase 연결 테스트
    const testConnection = async () => {
      try {
        await getDocs(collection(db, 'test'));
        setConnected(true);
      } catch (error) {
        console.error('Firebase 연결 실패:', error);
      }
    };

    testConnection();
  }, []);

  return (
    <div className="min-h-screen bg-gray-100 flex items-center justify-center">
      <div className="bg-white p-8 rounded-lg shadow-md text-center">
        <h1 className="text-2xl font-bold mb-4">Firebase 연결 테스트</h1>
        <div className={`p-4 rounded ${connected ? 'bg-green-100 text-green-800' : 'bg-yellow-100 text-yellow-800'}`}>
          {connected ? '✅ Firebase 연결 성공!' : '⏳ 연결 중...'}
        </div>
      </div>
    </div>
  );
}

export default App;
```

---

## ⚡ 4. Vercel 배포 환경

### 🏗 Vercel 계정 생성

#### 1. 계정 가입
1. [Vercel](https://vercel.com/) 접속
2. "Sign Up" 클릭
3. GitHub 계정으로 로그인 (권장)
4. Team 이름 설정: "[이름]-dev"

#### 2. GitHub 연동 확인
- GitHub 저장소 접근 권한 확인
- 필요 시 추가 권한 승인

### 🔧 Vercel CLI 설치

```bash
# npm을 통한 설치
npm install -g vercel

# 설치 확인
vercel --version

# Vercel 로그인
vercel login
# 이메일 입력 후 확인 이메일 클릭

# 로그인 확인
vercel whoami
```

### 🚀 첫 번째 배포 테스트

#### GitHub 저장소 생성
```bash
# 테스트 프로젝트 폴더에서
git init
git add .
git commit -m "feat: 초기 프로젝트 설정"

# GitHub에 새 저장소 생성 후
git remote add origin git@github.com:username/vibe-test-app.git
git branch -M main
git push -u origin main
```

#### Vercel 프로젝트 생성
```bash
# 프로젝트 폴더에서
vercel

# 설정 질문 응답:
# Set up and deploy? Y
# Which scope? [당신의 계정]
# Link to existing project? N
# Project name? vibe-test-app
# In which directory is your code located? ./
# Want to override settings? N
```

#### 자동 배포 확인
1. GitHub에 코드 push
2. Vercel 대시보드에서 배포 상태 확인
3. 배포된 URL 접속하여 테스트

### 🔐 환경변수 설정

#### Vercel 환경변수 추가
```bash
# Firebase 설정을 환경변수로 설정
vercel env add VITE_FIREBASE_API_KEY
vercel env add VITE_FIREBASE_AUTH_DOMAIN
vercel env add VITE_FIREBASE_PROJECT_ID
vercel env add VITE_FIREBASE_STORAGE_BUCKET
vercel env add VITE_FIREBASE_MESSAGING_SENDER_ID
vercel env add VITE_FIREBASE_APP_ID

# 각각에 대해 development, preview, production 환경 설정
```

#### 로컬 환경변수 설정
```bash
# .env.local 파일 생성
VITE_FIREBASE_API_KEY=your-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=your-app-id
```

---

## 🤖 5. AI 도구 설정

### 💬 ChatGPT 설정

#### 계정 및 API
1. [OpenAI](https://platform.openai.com/) 계정 생성
2. API 키 발급 (필요시)
3. 사용량 모니터링 설정

#### 효과적 사용법
- **명확한 역할 부여**: "당신은 React 전문 개발자입니다"
- **구체적 요구사항**: 기능, 스타일, 제약 조건 명시
- **예시 제공**: 원하는 결과물 샘플 첨부
- **단계별 질문**: 복잡한 문제는 단계로 나누어 질문

### 🚀 GitHub Copilot 설정

#### 구독 및 설치
1. [GitHub Copilot](https://github.com/features/copilot) 구독
2. VS Code에서 GitHub Copilot 확장프로그램 설치
3. GitHub 계정으로 로그인

#### 설정 최적화
```json
// VS Code settings.json에 추가
{
  "github.copilot.enable": {
    "*": true,
    "yaml": false,
    "plaintext": false,
    "markdown": false
  },
  "github.copilot.inlineSuggest.enable": true,
  "github.copilot.editor.enableAutoCompletions": true
}
```

### 🎯 Claude (Anthropic) 설정

#### 접속 및 사용
1. [Claude](https://claude.ai/) 접속
2. 계정 생성 (Google/이메일)
3. 무료 플랜으로 시작

#### 특화 사용법
- **긴 코드 분석**: ChatGPT보다 긴 컨텍스트 처리 우수
- **코드 리뷰**: 상세한 피드백과 개선 제안
- **문서 작성**: README, API 문서 작성

---

## 🛠 6. 문제 해결 가이드

### 🚨 일반적인 문제들

#### Node.js 관련 문제

**문제**: `node: command not found`
```bash
# 해결책 1: PATH 환경변수 확인
echo $PATH

# 해결책 2: 노드 재설치
# nvm 사용 시
nvm install node
nvm use node

# 직접 설치 시
# 공식 사이트에서 재다운로드
```

**문제**: `npm install` 권한 오류
```bash
# macOS/Linux
sudo chown -R $(whoami) ~/.npm

# 또는 nvm 사용으로 해결
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
```

**문제**: 패키지 설치 속도 느림
```bash
# 해결책 1: npm 레지스트리 변경
npm config set registry https://registry.npmjs.org/

# 해결책 2: pnpm 사용
npm install -g pnpm
pnpm install  # npm install 대신 사용
```

#### Git 관련 문제

**문제**: SSH 연결 실패
```bash
# 해결책 1: SSH 에이전트 재시작
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# 해결책 2: SSH 설정 확인
ssh -T git@github.com
```

**문제**: 커밋 실패 (author 설정)
```bash
# 해결책: 사용자 정보 재설정
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

#### Firebase 관련 문제

**문제**: Firebase 초기화 오류
```javascript
// 해결책: 환경변수 확인
console.log('Firebase Config:', {
  apiKey: import.meta.env.VITE_FIREBASE_API_KEY,
  projectId: import.meta.env.VITE_FIREBASE_PROJECT_ID
});

// .env.local 파일 존재 및 형식 확인
```

**문제**: Firestore 권한 오류
```javascript
// 해결책: 보안 규칙 임시 수정 (개발용)
// Firestore 콘솔 > Rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2024, 12, 31);
    }
  }
}
```

#### Vercel 배포 문제

**문제**: 빌드 실패
```bash
# 해결책 1: 로컬에서 빌드 테스트
npm run build

# 해결책 2: Vercel 환경변수 확인
vercel env ls
```

**문제**: 환경변수 인식 안됨
```bash
# 해결책: Vercel에서 환경변수 다시 설정
vercel env remove VITE_FIREBASE_API_KEY
vercel env add VITE_FIREBASE_API_KEY
```

### 🔧 성능 최적화

#### VS Code 성능 개선
```json
// settings.json
{
  "files.watcherExclude": {
    "**/node_modules/**": true,
    "**/.git/objects/**": true,
    "**/.git/subtree-cache/**": true,
    "**/dist/**": true,
    "**/build/**": true
  },
  "search.exclude": {
    "**/node_modules": true,
    "**/dist": true,
    "**/build": true
  }
}
```

#### 터미널 성능 개선
```bash
# Windows (PowerShell)
# Windows Terminal 설치 권장

# macOS
# iTerm2 설치 권장
brew install --cask iterm2

# Linux
# Zsh + Oh My Zsh 설치
sudo apt install zsh
sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
```

---

## ✅ 설정 완료 체크리스트

### 📋 기본 도구 설치
- [ ] Chrome 브라우저 + 필수 확장프로그램
- [ ] Visual Studio Code + 필수 확장프로그램
- [ ] Node.js (LTS 버전) + npm/pnpm
- [ ] Git + 사용자 설정
- [ ] SSH 키 생성 및 GitHub 등록

### 🔥 Firebase 설정
- [ ] Firebase 프로젝트 생성
- [ ] Authentication, Firestore, Storage 활성화
- [ ] Firebase CLI 설치 및 로그인
- [ ] 테스트 앱에서 연결 확인

### ⚡ Vercel 설정
- [ ] Vercel 계정 생성 및 GitHub 연동
- [ ] Vercel CLI 설치 및 로그인
- [ ] 테스트 배포 성공
- [ ] 환경변수 설정 완료

### 🤖 AI 도구 설정
- [ ] ChatGPT 계정 생성
- [ ] GitHub Copilot 구독 및 설치
- [ ] Claude 계정 생성 (선택)

### 🧪 최종 테스트
- [ ] 새 React 프로젝트 생성
- [ ] Firebase 연동 확인
- [ ] GitHub push 및 Vercel 자동 배포
- [ ] 배포된 사이트 정상 작동

---

## 📞 지원 및 문의

### 💬 커뮤니티 지원
- **Discord**: [Vibe Academy 서버](링크)
- **GitHub Discussions**: [토론 게시판](링크)
- **YouTube 댓글**: 각 영상별 질의응답

### 📧 직접 문의
- **이메일**: academy@baikalsystems.com
- **카카오톡**: [오픈채팅방](링크)

### 📚 추가 학습 자료
- [공식 문서 모음](링크)
- [문제 해결 FAQ](링크)
- [업데이트 노트](링크)

---

**다음**: 🏆 평가 및 인증 시스템 설계
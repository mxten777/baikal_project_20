# 🔧 프로젝트별 환경 설정 가이드

**Vibe Academy 핵심 기술 스택별 상세 설정**

---

## 📋 목차

1. [Vite + React 프로젝트 설정](#1-vite--react-프로젝트-설정)
2. [TailwindCSS 완벽 설정](#2-tailwindcss-완벽-설정)
3. [Firebase 고급 설정](#3-firebase-고급-설정)
4. [Vercel 배포 최적화](#4-vercel-배포-최적화)
5. [개발 워크플로우 설정](#5-개발-워크플로우-설정)
6. [프로젝트 템플릿](#6-프로젝트-템플릿)

---

## ⚡ 1. Vite + React 프로젝트 설정

### 🏗 프로젝트 생성 및 기본 설정

#### 새 프로젝트 생성
```bash
# 1. 프로젝트 생성
npm create vite@latest my-vibe-project -- --template react
cd my-vibe-project

# 2. 의존성 설치
npm install

# 3. 개발 서버 실행 테스트
npm run dev
# http://localhost:5173 접속 확인
```

#### package.json 최적화
```json
{
  "name": "my-vibe-project",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "deploy": "npm run build && vercel --prod"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "firebase": "^10.7.1",
    "react-router-dom": "^6.20.1",
    "lucide-react": "^0.294.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.37",
    "@types/react-dom": "^18.2.15",
    "@vitejs/plugin-react": "^4.1.1",
    "autoprefixer": "^10.4.16",
    "eslint": "^8.53.0",
    "eslint-plugin-react": "^7.33.2",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.4",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.3.6",
    "vite": "^4.5.0"
  }
}
```

#### vite.config.js 고급 설정
```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src'),
      '@components': path.resolve(__dirname, 'src/components'),
      '@pages': path.resolve(__dirname, 'src/pages'),
      '@utils': path.resolve(__dirname, 'src/utils'),
      '@hooks': path.resolve(__dirname, 'src/hooks'),
      '@contexts': path.resolve(__dirname, 'src/contexts'),
      '@assets': path.resolve(__dirname, 'src/assets')
    }
  },
  server: {
    port: 3000,
    host: true,
    open: true
  },
  build: {
    outDir: 'dist',
    sourcemap: false,
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom'],
          firebase: ['firebase/app', 'firebase/auth', 'firebase/firestore']
        }
      }
    }
  },
  preview: {
    port: 4173,
    host: true,
    open: true
  }
})
```

### 📁 폴더 구조 설정

#### 표준 폴더 구조 생성
```bash
# 프로젝트 루트에서 실행
mkdir -p src/{components,pages,hooks,contexts,utils,assets,styles}
mkdir -p src/components/{ui,layout,features}
mkdir -p src/assets/{images,icons}
mkdir -p public/{images,icons}

# 기본 파일 생성
touch src/components/index.js
touch src/utils/index.js
touch src/hooks/index.js
touch src/contexts/index.js
```

#### 파일 구조 예시
```
src/
├── components/           # 재사용 가능한 컴포넌트
│   ├── ui/              # 기본 UI 컴포넌트
│   │   ├── Button.jsx
│   │   ├── Input.jsx
│   │   └── Modal.jsx
│   ├── layout/          # 레이아웃 컴포넌트
│   │   ├── Header.jsx
│   │   ├── Footer.jsx
│   │   └── Sidebar.jsx
│   └── features/        # 기능별 컴포넌트
│       ├── auth/
│       └── dashboard/
├── pages/               # 페이지 컴포넌트
│   ├── Home.jsx
│   ├── Login.jsx
│   └── Dashboard.jsx
├── hooks/               # 커스텀 훅
│   ├── useAuth.js
│   └── useFirestore.js
├── contexts/            # React Context
│   ├── AuthContext.jsx
│   └── ThemeContext.jsx
├── utils/               # 유틸리티 함수
│   ├── helpers.js
│   └── constants.js
├── assets/              # 정적 자산
│   ├── images/
│   └── icons/
└── styles/              # 스타일 파일
    ├── globals.css
    └── components.css
```

---

## 🎨 2. TailwindCSS 완벽 설정

### 🚀 설치 및 기본 설정

#### TailwindCSS 설치
```bash
# TailwindCSS 설치
npm install -D tailwindcss postcss autoprefixer

# 설정 파일 생성
npx tailwindcss init -p
```

#### tailwind.config.js 고급 설정
```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // Vibe Academy 브랜드 컬러
        primary: {
          50: '#eff6ff',
          500: '#3b82f6',
          600: '#2563eb',
          700: '#1d4ed8',
          900: '#1e3a8a',
        },
        secondary: {
          50: '#f0f9ff',
          500: '#06b6d4',
          600: '#0891b2',
          700: '#0e7490',
        },
        accent: {
          50: '#fdf4ff',
          500: '#a855f7',
          600: '#9333ea',
          700: '#7c3aed',
        },
        gray: {
          50: '#f9fafb',
          100: '#f3f4f6',
          200: '#e5e7eb',
          300: '#d1d5db',
          400: '#9ca3af',
          500: '#6b7280',
          600: '#4b5563',
          700: '#374151',
          800: '#1f2937',
          900: '#111827',
        }
      },
      fontFamily: {
        sans: ['Inter', 'ui-sans-serif', 'system-ui'],
        mono: ['Fira Code', 'ui-monospace', 'monospace'],
      },
      fontSize: {
        'xs': ['0.75rem', { lineHeight: '1rem' }],
        'sm': ['0.875rem', { lineHeight: '1.25rem' }],
        'base': ['1rem', { lineHeight: '1.5rem' }],
        'lg': ['1.125rem', { lineHeight: '1.75rem' }],
        'xl': ['1.25rem', { lineHeight: '1.75rem' }],
        '2xl': ['1.5rem', { lineHeight: '2rem' }],
        '3xl': ['1.875rem', { lineHeight: '2.25rem' }],
        '4xl': ['2.25rem', { lineHeight: '2.5rem' }],
      },
      spacing: {
        '18': '4.5rem',
        '88': '22rem',
        '128': '32rem',
      },
      borderRadius: {
        'xl': '0.75rem',
        '2xl': '1rem',
        '3xl': '1.5rem',
      },
      boxShadow: {
        'inner-lg': 'inset 0 2px 4px 0 rgba(0, 0, 0, 0.1)',
        'glow': '0 0 20px rgba(59, 130, 246, 0.5)',
      },
      animation: {
        'fade-in': 'fadeIn 0.5s ease-in-out',
        'slide-up': 'slideUp 0.3s ease-out',
        'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        slideUp: {
          '0%': { transform: 'translateY(10px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        }
      }
    },
  },
  plugins: [
    require('@tailwindcss/forms'),
    require('@tailwindcss/typography'),
    require('@tailwindcss/aspect-ratio'),
  ],
}
```

#### CSS 파일 설정
```css
/* src/styles/globals.css */
@tailwind base;
@tailwind components;
@tailwind utilities;

/* 기본 스타일 설정 */
@layer base {
  html {
    font-family: 'Inter', system-ui, sans-serif;
  }
  
  body {
    @apply bg-gray-50 text-gray-900;
  }
  
  h1, h2, h3, h4, h5, h6 {
    @apply font-semibold text-gray-900;
  }
}

/* 컴포넌트 스타일 */
@layer components {
  .btn {
    @apply px-4 py-2 rounded-lg font-medium transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-offset-2;
  }
  
  .btn-primary {
    @apply bg-primary-600 text-white hover:bg-primary-700 focus:ring-primary-500;
  }
  
  .btn-secondary {
    @apply bg-gray-200 text-gray-900 hover:bg-gray-300 focus:ring-gray-500;
  }
  
  .input {
    @apply block w-full px-3 py-2 border border-gray-300 rounded-lg shadow-sm focus:ring-primary-500 focus:border-primary-500;
  }
  
  .card {
    @apply bg-white rounded-lg shadow-sm border border-gray-200 p-6;
  }
  
  .container-responsive {
    @apply container mx-auto px-4 sm:px-6 lg:px-8;
  }
}

/* 유틸리티 클래스 */
@layer utilities {
  .text-gradient {
    @apply bg-gradient-to-r from-primary-600 to-secondary-600 bg-clip-text text-transparent;
  }
  
  .bg-gradient-primary {
    @apply bg-gradient-to-r from-primary-500 to-secondary-500;
  }
  
  .shadow-glow {
    box-shadow: 0 0 20px rgba(59, 130, 246, 0.3);
  }
}
```

### 🎯 추가 플러그인 설치

```bash
# 유용한 TailwindCSS 플러그인들
npm install -D @tailwindcss/forms @tailwindcss/typography @tailwindcss/aspect-ratio

# 헤드리스 UI (권장)
npm install @headlessui/react @heroicons/react

# 추가 아이콘 라이브러리
npm install lucide-react
```

---

## 🔥 3. Firebase 고급 설정

### 🔧 Firebase 설정 파일 구조화

#### 환경변수 설정
```bash
# .env.local
VITE_FIREBASE_API_KEY=your-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id  
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=your-app-id
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# 개발/프로덕션 환경 구분
VITE_NODE_ENV=development
```

#### Firebase 설정 모듈화
```javascript
// src/config/firebase.js
import { initializeApp } from 'firebase/app';
import { getAuth, connectAuthEmulator } from 'firebase/auth';
import { getFirestore, connectFirestoreEmulator } from 'firebase/firestore';
import { getStorage, connectStorageEmulator } from 'firebase/storage';
import { getAnalytics } from 'firebase/analytics';

const firebaseConfig = {
  apiKey: import.meta.env.VITE_FIREBASE_API_KEY,
  authDomain: import.meta.env.VITE_FIREBASE_AUTH_DOMAIN,
  projectId: import.meta.env.VITE_FIREBASE_PROJECT_ID,
  storageBucket: import.meta.env.VITE_FIREBASE_STORAGE_BUCKET,
  messagingSenderId: import.meta.env.VITE_FIREBASE_MESSAGING_SENDER_ID,
  appId: import.meta.env.VITE_FIREBASE_APP_ID,
  measurementId: import.meta.env.VITE_FIREBASE_MEASUREMENT_ID
};

// Firebase 앱 초기화
const app = initializeApp(firebaseConfig);

// 서비스 초기화
export const auth = getAuth(app);
export const db = getFirestore(app);
export const storage = getStorage(app);

// Analytics (프로덕션에서만)
export const analytics = typeof window !== 'undefined' && import.meta.env.PROD 
  ? getAnalytics(app) 
  : null;

// 개발 환경에서 에뮬레이터 연결 (한 번만 실행)
if (import.meta.env.DEV && !globalThis.__FIREBASE_EMULATOR_CONNECTED__) {
  try {
    connectAuthEmulator(auth, 'http://localhost:9099');
    connectFirestoreEmulator(db, 'localhost', 8080);
    connectStorageEmulator(storage, 'localhost', 9199);
    globalThis.__FIREBASE_EMULATOR_CONNECTED__ = true;
  } catch (error) {
    console.log('Firebase emulator already connected');
  }
}

export default app;
```

#### Firebase 서비스 래퍼
```javascript
// src/services/firebaseService.js
import { 
  createUserWithEmailAndPassword,
  signInWithEmailAndPassword,
  signOut,
  updateProfile,
  sendPasswordResetEmail,
  onAuthStateChanged
} from 'firebase/auth';
import {
  collection,
  doc,
  addDoc,
  updateDoc,
  deleteDoc,
  getDoc,
  getDocs,
  query,
  where,
  orderBy,
  limit,
  onSnapshot
} from 'firebase/firestore';
import {
  ref,
  uploadBytes,
  getDownloadURL,
  deleteObject
} from 'firebase/storage';
import { auth, db, storage } from '@/config/firebase';

// Auth 서비스
export const authService = {
  // 회원가입
  signup: async (email, password, displayName) => {
    const { user } = await createUserWithEmailAndPassword(auth, email, password);
    await updateProfile(user, { displayName });
    return user;
  },

  // 로그인
  signin: (email, password) => signInWithEmailAndPassword(auth, email, password),

  // 로그아웃
  signout: () => signOut(auth),

  // 비밀번호 재설정
  resetPassword: (email) => sendPasswordResetEmail(auth, email),

  // 인증 상태 변경 감지
  onAuthStateChange: (callback) => onAuthStateChanged(auth, callback),

  // 현재 사용자
  getCurrentUser: () => auth.currentUser
};

// Firestore 서비스
export const firestoreService = {
  // 문서 생성
  create: (collectionName, data) => addDoc(collection(db, collectionName), data),

  // 문서 읽기
  read: (collectionName, docId) => getDoc(doc(db, collectionName, docId)),

  // 문서 업데이트
  update: (collectionName, docId, data) => updateDoc(doc(db, collectionName, docId), data),

  // 문서 삭제
  delete: (collectionName, docId) => deleteDoc(doc(db, collectionName, docId)),

  // 컬렉션 쿼리
  query: (collectionName, constraints = []) => {
    const q = query(collection(db, collectionName), ...constraints);
    return getDocs(q);
  },

  // 실시간 리스너
  listen: (collectionName, constraints = [], callback) => {
    const q = query(collection(db, collectionName), ...constraints);
    return onSnapshot(q, callback);
  }
};

// Storage 서비스
export const storageService = {
  // 파일 업로드
  upload: async (path, file) => {
    const storageRef = ref(storage, path);
    const snapshot = await uploadBytes(storageRef, file);
    return getDownloadURL(snapshot.ref);
  },

  // 파일 삭제
  delete: (path) => deleteObject(ref(storage, path)),

  // 다운로드 URL 얻기
  getDownloadURL: (path) => getDownloadURL(ref(storage, path))
};
```

### 🔒 보안 규칙 설정

#### Firestore 보안 규칙
```javascript
// firestore.rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 사용자 프로필 - 본인만 읽기/쓰기
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // 프로젝트 - 작성자만 수정, 모든 인증된 사용자 읽기
    match /projects/{projectId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == resource.data.authorId;
    }
    
    // 댓글 - 작성자만 수정/삭제, 모든 인증된 사용자 읽기/생성
    match /comments/{commentId} {
      allow read, create: if request.auth != null;
      allow update, delete: if request.auth != null && request.auth.uid == resource.data.authorId;
    }
  }
}
```

#### Storage 보안 규칙
```javascript
// storage.rules
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 사용자별 폴더 - 본인만 접근
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // 공개 이미지 - 모든 인증된 사용자 읽기, 업로드는 본인만
    match /public/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null 
        && request.resource.size < 5 * 1024 * 1024  // 5MB 제한
        && request.resource.contentType.matches('image/.*');
    }
  }
}
```

---

## ⚡ 4. Vercel 배포 최적화

### 🔧 Vercel 설정 파일

#### vercel.json 설정
```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "app/api/**/*.js": {
      "runtime": "nodejs18.x"
    }
  },
  "redirects": [
    {
      "source": "/home",
      "destination": "/"
    }
  ],
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Access-Control-Allow-Origin",
          "value": "*"
        },
        {
          "key": "Access-Control-Allow-Methods",
          "value": "GET, POST, PUT, DELETE, OPTIONS"
        },
        {
          "key": "Access-Control-Allow-Headers",
          "value": "Content-Type, Authorization"
        }
      ]
    }
  ]
}
```

#### 환경변수 자동 설정 스크립트
```javascript
// scripts/setup-vercel-env.js
import { execSync } from 'child_process';
import fs from 'fs';
import path from 'path';

const envFile = path.join(process.cwd(), '.env.local');

if (fs.existsSync(envFile)) {
  const envContent = fs.readFileSync(envFile, 'utf8');
  const envVars = envContent
    .split('\n')
    .filter(line => line.includes('=') && !line.startsWith('#'))
    .map(line => {
      const [key, ...values] = line.split('=');
      return { key: key.trim(), value: values.join('=').trim() };
    });

  console.log('Setting up Vercel environment variables...');
  
  envVars.forEach(({ key, value }) => {
    try {
      // development, preview, production 환경에 모두 설정
      ['development', 'preview', 'production'].forEach(env => {
        execSync(`vercel env add ${key} ${env}`, {
          input: value,
          stdio: ['pipe', 'inherit', 'inherit']
        });
      });
      console.log(`✅ ${key} set for all environments`);
    } catch (error) {
      console.log(`⚠️ ${key} might already exist`);
    }
  });
  
  console.log('Environment variables setup complete!');
} else {
  console.error('❌ .env.local file not found');
}
```

### 🚀 빌드 최적화

#### Vite 빌드 최적화
```javascript
// vite.config.js 추가 설정
export default defineConfig({
  // ... 기존 설정
  build: {
    target: 'esnext',
    minify: 'terser',
    cssMinify: true,
    rollupOptions: {
      output: {
        manualChunks: {
          'vendor-react': ['react', 'react-dom'],
          'vendor-firebase': ['firebase/app', 'firebase/auth', 'firebase/firestore'],
          'vendor-router': ['react-router-dom'],
          'vendor-ui': ['@headlessui/react', 'lucide-react']
        },
        assetFileNames: (assetInfo) => {
          const info = assetInfo.name.split('.');
          const ext = info[info.length - 1];
          if (/png|jpe?g|svg|gif|tiff|bmp|ico/i.test(ext)) {
            return `assets/images/[name]-[hash][extname]`;
          }
          if (/css/i.test(ext)) {
            return `assets/css/[name]-[hash][extname]`;
          }
          return `assets/[name]-[hash][extname]`;
        },
        chunkFileNames: 'assets/js/[name]-[hash].js',
        entryFileNames: 'assets/js/[name]-[hash].js'
      }
    },
    terserOptions: {
      compress: {
        drop_console: true,
        drop_debugger: true
      }
    }
  }
});
```

---

## 🔄 5. 개발 워크플로우 설정

### 📝 ESLint 및 Prettier 설정

#### .eslintrc.cjs
```javascript
module.exports = {
  root: true,
  env: { browser: true, es2020: true },
  extends: [
    'eslint:recommended',
    '@typescript-eslint/recommended',
    'plugin:react/recommended',
    'plugin:react/jsx-runtime',
    'plugin:react-hooks/recommended',
  ],
  ignorePatterns: ['dist', '.eslintrc.cjs'],
  parser: '@typescript-eslint/parser',
  plugins: ['react-refresh'],
  rules: {
    'react-refresh/only-export-components': [
      'warn',
      { allowConstantExport: true },
    ],
    'react/prop-types': 'off',
    'react/react-in-jsx-scope': 'off',
    '@typescript-eslint/no-unused-vars': ['warn'],
    'no-console': ['warn', { allow: ['warn', 'error'] }],
    'prefer-const': 'error',
    'no-var': 'error'
  },
}
```

#### .prettierrc
```json
{
  "semi": true,
  "trailingComma": "es5",
  "singleQuote": true,
  "printWidth": 80,
  "tabWidth": 2,
  "useTabs": false,
  "bracketSpacing": true,
  "bracketSameLine": false,
  "arrowParens": "avoid",
  "endOfLine": "lf"
}
```

### 🔧 Git 훅 설정

#### Husky 설치 및 설정
```bash
# Husky 설치
npm install -D husky lint-staged

# Husky 초기화
npx husky install

# pre-commit 훅 생성
npx husky add .husky/pre-commit "npx lint-staged"
```

#### package.json에 lint-staged 설정 추가
```json
{
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,css,md}": [
      "prettier --write"
    ]
  }
}
```

---

## 📦 6. 프로젝트 템플릿

### 🎯 완전한 프로젝트 생성 스크립트

```bash
#!/bin/bash
# create-vibe-project.sh

PROJECT_NAME=$1

if [ -z "$PROJECT_NAME" ]; then
  echo "Usage: ./create-vibe-project.sh <project-name>"
  exit 1
fi

echo "🚀 Creating Vibe Academy project: $PROJECT_NAME"

# 1. Vite React 프로젝트 생성
npm create vite@latest $PROJECT_NAME -- --template react
cd $PROJECT_NAME

# 2. 의존성 설치
npm install
npm install firebase react-router-dom @headlessui/react @heroicons/react lucide-react
npm install -D tailwindcss postcss autoprefixer @tailwindcss/forms @tailwindcss/typography eslint-config-prettier prettier husky lint-staged

# 3. TailwindCSS 초기화
npx tailwindcss init -p

# 4. 폴더 구조 생성
mkdir -p src/{components,pages,hooks,contexts,utils,assets,styles,config,services}
mkdir -p src/components/{ui,layout,features}
mkdir -p src/assets/{images,icons}
mkdir -p public/{images,icons}

# 5. Git 초기화
git init
git add .
git commit -m "feat: 초기 프로젝트 설정"

# 6. Husky 설정
npx husky install
npx husky add .husky/pre-commit "npx lint-staged"

echo "✅ Project $PROJECT_NAME created successfully!"
echo "📁 Navigate to the project: cd $PROJECT_NAME"
echo "🔥 Start development: npm run dev"
echo "📋 Don't forget to:"
echo "   - Set up Firebase project"
echo "   - Configure environment variables"
echo "   - Set up Vercel deployment"
```

### 📋 프로젝트 체크리스트

#### 프로젝트 시작 체크리스트
```markdown
## 새 프로젝트 시작 체크리스트

### 기본 설정
- [ ] Vite + React 프로젝트 생성
- [ ] TailwindCSS 설치 및 설정
- [ ] 폴더 구조 생성
- [ ] ESLint, Prettier 설정
- [ ] Git 초기화 및 첫 커밋

### Firebase 설정
- [ ] Firebase 프로젝트 생성
- [ ] Authentication 활성화
- [ ] Firestore Database 생성
- [ ] Storage 설정
- [ ] 환경변수 파일 생성 (.env.local)
- [ ] Firebase SDK 설치 및 초기화

### 배포 설정
- [ ] GitHub 저장소 생성
- [ ] Vercel 프로젝트 연결
- [ ] 환경변수 Vercel에 설정
- [ ] 첫 배포 테스트

### 개발 환경
- [ ] VS Code 확장프로그램 설치
- [ ] Git 훅 설정 (Husky)
- [ ] 개발 서버 실행 테스트
- [ ] 라우팅 설정 (React Router)

### 추가 설정
- [ ] 로딩 상태 컴포넌트
- [ ] 에러 바운더리 설정
- [ ] 기본 레이아웃 컴포넌트
- [ ] 인증 컨텍스트 설정
- [ ] 기본 페이지 컴포넌트
```

---

## 🎯 마무리

이 가이드를 따라하면 Vibe Academy 스타일의 완전한 개발 환경을 구축할 수 있습니다. 각 단계를 차근차근 따라하시고, 문제가 생기면 [문제 해결 가이드](개발환경_구축_완벽가이드.md#6-문제-해결-가이드)를 참조하세요.

**다음 단계**: 실제 MVP 프로젝트 개발 시작!
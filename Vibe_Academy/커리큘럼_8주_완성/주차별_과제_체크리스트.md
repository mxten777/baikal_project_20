# 📋 주차별 과제 및 체크리스트

**각 주차별 상세 과제와 평가 기준**

---

## 🎯 1주차 과제: 기초 & 환경 구축

### 필수 과제

#### 과제 1-1: 개발환경 완전 구축 ✅
**마감**: 1주차 화요일 오후 6시

**요구사항**:
- [ ] Node.js v18 이상 설치
- [ ] VS Code + 필수 확장프로그램 설치
- [ ] Git 설정 (사용자명, 이메일)
- [ ] GitHub 계정 생성 및 SSH 키 등록
- [ ] Firebase 프로젝트 생성
- [ ] Vercel 계정 생성 및 GitHub 연동

**제출물**:
- 환경 구축 완료 스크린샷 (Notion 페이지)
- GitHub 프로필 URL

**평가 기준**:
- 모든 도구 정상 설치: 15점
- GitHub 연동 완료: 10점

#### 과제 1-2: Hello World MVP 배포 🚀
**마감**: 1주차 수요일 오후 8시

**요구사항**:
```bash
# 프로젝트 생성 및 설정
npm create vite@latest [이름]-hello-world -- --template react
cd [이름]-hello-world
npm install
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# Git 설정
git init
git add .
git commit -m "feat: 초기 프로젝트 설정"
git remote add origin [GitHub-URL]
git push -u origin main

# Vercel 배포
npx vercel --prod
```

**기능 요구사항**:
- [ ] 개인 소개 페이지
- [ ] 반응형 디자인 (모바일/데스크톱)
- [ ] 최소 3개 섹션 (헤더, 메인, 푸터)
- [ ] Tailwind CSS 활용
- [ ] 배포 완료 및 URL 동작 확인

**제출물**:
- GitHub 저장소 URL
- Vercel 배포 URL
- 개발 과정 일지 (Notion)

**평가 기준**:
- 코드 품질: 20점
- 디자인 완성도: 15점
- 배포 성공: 15점

#### 과제 1-3: 학습 계획서 작성 📝
**마감**: 1주차 금요일 오후 6시

**내용**:
```markdown
# 개인 학습 계획서

## 1. 개인 정보
- 이름: 
- 직책/역할:
- 개발 경험:
- 목표:

## 2. MVP 아이디어 후보 (3개)
### 아이디어 1: [제목]
- 문제: 
- 해결책:
- 타겟 사용자:

### 아이디어 2: [제목]
- 문제:
- 해결책:
- 타겟 사용자:

### 아이디어 3: [제목]
- 문제:
- 해결책:
- 타겟 사용자:

## 3. 8주 개인 목표
- 기술적 목표:
- 비즈니스 목표:
- 개인 성장 목표:

## 4. 학습 일정
- 주중 학습 시간:
- 주말 학습 시간:
- 선호 학습 방식:
```

**평가 기준**:
- 계획의 구체성: 15점
- 아이디어 타당성: 10점

### 선택 과제

#### 보너스 과제 1-1: 기술 블로그 시작 ✨
**보너스 점수**: +5점

**요구사항**:
- 기술 블로그 개설 (Medium, velog, tistory 등)
- 1주차 학습 회고록 작성 (800자 이상)
- 개발환경 구축 가이드 작성

#### 보너스 과제 1-2: 오픈소스 기여 ✨
**보너스 점수**: +3점

**요구사항**:
- GitHub 프로젝트 1개 Star
- 오픈소스 프로젝트 Issue 또는 Discussion 참여
- 기여 내용 정리

---

## 🎯 2주차 과제: MVP 기획 & 프롬프트

### 필수 과제

#### 과제 2-1: 1페이지 프롬프트 완성 📋
**마감**: 2주차 수요일 오후 8시

**템플릿 활용**:
```markdown
# [프로젝트명] MVP 프롬프트

## 🎯 문제 정의
**타겟 사용자**: 
**핵심 문제**: 
**문제 발생 빈도**: 
**현재 해결책의 한계**: 

## ✨ 솔루션
**핵심 가치 제안**: [한 문장]
**차별화 포인트**: 

**주요 기능**:
1. **[기능명 1]**: [구체적 설명]
   - 사용자 액션: 
   - 시스템 응답:
   - 예상 결과:

2. **[기능명 2]**: [구체적 설명]
   - 사용자 액션:
   - 시스템 응답:
   - 예상 결과:

3. **[기능명 3]**: [구체적 설명]
   - 사용자 액션:
   - 시스템 응답:
   - 예상 결과:

## 🎨 사용자 경험
**첫 방문 시나리오**:
1. 사용자가 사이트에 접속한다
2. [구체적 단계]
3. [구체적 단계]
4. 가치를 경험한다

**일반 사용 시나리오**:
1. [단계 1]
2. [단계 2]  
3. [단계 3]

## 🔧 기술 구현
**Frontend**: Vite + React + Tailwind CSS
**Backend**: Firebase (Auth + Firestore + Storage)
**배포**: Vercel
**외부 API**: [필요시 명시]
**특별 기능**: [실시간, 알림, 파일 업로드 등]

## 📊 성공 지표
**출시 후 30일 목표**:
- 등록 사용자: [숫자]명
- 일일 활성 사용자: [숫자]명
- 핵심 기능 사용률: [숫자]%
- 사용자 만족도: [NPS 점수]

**기술적 목표**:
- 페이지 로딩 속도: 3초 이내
- 모바일 사용성: 90% 이상
- 버그 발생률: 5% 이하

## 🚀 개발 일정
**1주차**: UI/UX 디자인 및 기본 구조
**2주차**: 핵심 기능 구현 및 Firebase 연동
**3주차**: 추가 기능 및 최적화
**4주차**: 테스트 및 배포

## 💡 향후 확장 계획
**Phase 2**: [추가 기능]
**Phase 3**: [비즈니스 모델]
**Phase 4**: [시장 확장]
```

**평가 기준**:
- 문제 정의 명확성: 25점
- 솔루션 구체성: 25점
- 기술적 타당성: 20점
- 성공 지표 현실성: 10점

#### 과제 2-2: 경쟁사 분석 리포트 🔍
**마감**: 2주차 목요일 오후 6시

**분석 대상**: 유사 서비스 3개

**분석 템플릿**:
```markdown
# 경쟁사 분석 리포트

## 경쟁사 1: [서비스명]
**URL**: 
**주요 기능**: 
**장점**: 
**단점**: 
**타겟 사용자**: 
**수익 모델**: 
**차별화 포인트**: 

## 경쟁사 2: [서비스명]
[동일한 형식]

## 경쟁사 3: [서비스명]
[동일한 형식]

## 종합 분석
**시장 현황**: 
**기회 요소**: 
**위험 요소**: 
**우리의 차별화 전략**: 

## 벤치마킹 요소
**도입할 기능**: 
**개선할 UX**: 
**피할 실수**: 
```

**평가 기준**:
- 분석 깊이: 15점
- 차별화 전략: 10점

#### 과제 2-3: 프롬프트 템플릿 라이브러리 🤖
**마감**: 2주차 금요일 오후 8시

**요구사항**:
개인 MVP 개발에 필요한 프롬프트 템플릿 5개 작성

**템플릿 예시**:
```markdown
## 1. React 컴포넌트 생성 프롬프트
당신은 React 전문 개발자입니다.

다음 요구사항에 따라 [컴포넌트명] 컴포넌트를 생성해주세요:

기능: [상세 기능 설명]
스타일: Tailwind CSS 사용
상태 관리: useState 또는 useReducer
props: [필요한 props 정의]

출력 형식:
- 완전한 컴포넌트 코드
- Props 타입 정의 (TypeScript)
- 사용 예시

## 2. Firebase 연동 프롬프트
[템플릿 내용]

## 3. UI 디자인 프롬프트
[템플릿 내용]

## 4. API 설계 프롬프트
[템플릿 내용]

## 5. 테스트 코드 프롬프트
[템플릿 내용]
```

**평가 기준**:
- 템플릿 품질: 15점
- 실용성: 10점

### 선택 과제

#### 보너스 과제 2-1: 사용자 인터뷰 진행 ✨
**보너스 점수**: +10점

**요구사항**:
- 타겟 사용자 3명 이상 인터뷰
- 인터뷰 스크립트 작성
- 인터뷰 결과 정리 및 인사이트 도출
- MVP 기획에 반영

---

## 🎯 3주차 과제: Frontend 개발

### 필수 과제

#### 과제 3-1: 컴포넌트 구조 설계 🏗️
**마감**: 3주차 화요일 오후 6시

**요구사항**:
```
src/
├── components/
│   ├── common/          # 공통 컴포넌트
│   │   ├── Button/
│   │   │   ├── index.jsx
│   │   │   └── Button.stories.js (선택)
│   │   ├── Input/
│   │   ├── Modal/
│   │   └── Loading/
│   ├── layout/          # 레이아웃 컴포넌트
│   │   ├── Header/
│   │   ├── Sidebar/
│   │   ├── Footer/
│   │   └── Layout/
│   └── features/        # 기능별 컴포넌트
│       └── [FeatureName]/
│           ├── index.jsx
│           ├── components/
│           └── hooks/
├── hooks/               # 커스텀 훅
├── utils/               # 유틸리티 함수
├── constants/           # 상수
└── types/              # 타입 정의 (TypeScript)
```

**제출물**:
- 컴포넌트 구조도 (다이어그램)
- 각 컴포넌트별 역할 정의
- Props 인터페이스 설계

#### 과제 3-2: 재사용 컴포넌트 구현 🧩
**마감**: 3주차 수요일 오후 8시

**구현 컴포넌트**:
1. **Button 컴포넌트**
```jsx
// components/common/Button/index.jsx
import PropTypes from 'prop-types';

const Button = ({ 
  children, 
  variant = 'primary', 
  size = 'medium',
  disabled = false,
  loading = false,
  onClick,
  ...props 
}) => {
  const baseClasses = 'font-medium rounded-lg transition-colors focus:outline-none focus:ring-2';
  
  const variants = {
    primary: 'bg-blue-600 text-white hover:bg-blue-700 focus:ring-blue-500',
    secondary: 'bg-gray-200 text-gray-900 hover:bg-gray-300 focus:ring-gray-500',
    danger: 'bg-red-600 text-white hover:bg-red-700 focus:ring-red-500'
  };

  const sizes = {
    small: 'px-3 py-1.5 text-sm',
    medium: 'px-4 py-2 text-sm',
    large: 'px-6 py-3 text-base'
  };

  return (
    <button
      className={`${baseClasses} ${variants[variant]} ${sizes[size]} ${
        disabled ? 'opacity-50 cursor-not-allowed' : ''
      }`}
      disabled={disabled || loading}
      onClick={onClick}
      {...props}
    >
      {loading ? (
        <div className="flex items-center">
          <svg className="animate-spin -ml-1 mr-2 h-4 w-4" fill="none" viewBox="0 0 24 24">
            <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4"></circle>
            <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
          </svg>
          Loading...
        </div>
      ) : children}
    </button>
  );
};

Button.propTypes = {
  children: PropTypes.node.isRequired,
  variant: PropTypes.oneOf(['primary', 'secondary', 'danger']),
  size: PropTypes.oneOf(['small', 'medium', 'large']),
  disabled: PropTypes.bool,
  loading: PropTypes.bool,
  onClick: PropTypes.func
};

export default Button;
```

2. **Input 컴포넌트**
3. **Modal 컴포넌트**
4. **Loading 컴포넌트**

**평가 기준**:
- 재사용성: 20점
- 접근성 고려: 15점
- 코드 품질: 15점

#### 과제 3-3: 메인 페이지 구현 🎨
**마감**: 3주차 금요일 오후 8시

**요구사항**:
- [ ] 반응형 디자인 (모바일/태블릿/데스크톱)
- [ ] 접근성 고려 (semantic HTML, ARIA)
- [ ] 부드러운 애니메이션 (Framer Motion 또는 CSS)
- [ ] 로딩 상태 처리
- [ ] 에러 상태 처리

**포함 요소**:
- 히어로 섹션
- 핵심 기능 소개
- 사용자 피드백 (가상)
- CTA (Call to Action)
- 푸터

**성능 요구사항**:
- Lighthouse 점수 90점 이상
- 첫 화면 로딩 시간 3초 이내
- 이미지 최적화 적용

### 선택 과제

#### 보너스 과제 3-1: 스토리북 구축 ✨
**보너스 점수**: +15점

**요구사항**:
- Storybook 설치 및 설정
- 재사용 컴포넌트별 스토리 작성
- 다양한 상태(variant, size 등) 스토리
- 문서화 추가

#### 보너스 과제 3-2: 테스트 코드 작성 ✨
**보너스 점수**: +10점

**요구사항**:
- React Testing Library 사용
- 컴포넌트별 유닛 테스트
- 통합 테스트 1개 이상
- 커버리지 80% 이상

---

## 🎯 4주차 과제: Backend 연동

### 필수 과제

#### 과제 4-1: Firebase Authentication 구현 🔐
**마감**: 4주차 화요일 오후 8시

**기능 요구사항**:
- [ ] 이메일/비밀번호 회원가입
- [ ] 이메일/비밀번호 로그인
- [ ] Google 소셜 로그인
- [ ] 로그아웃
- [ ] 비밀번호 재설정
- [ ] 프로필 정보 수정

**구현 예시**:
```jsx
// hooks/useAuth.js
import { useState, useEffect, createContext, useContext } from 'react';
import { 
  auth,
  createUserWithEmailAndPassword,
  signInWithEmailAndPassword,
  signOut,
  onAuthStateChanged,
  GoogleAuthProvider,
  signInWithPopup,
  sendPasswordResetEmail,
  updateProfile
} from '../firebase';

const AuthContext = createContext();

export function useAuth() {
  return useContext(AuthContext);
}

export function AuthProvider({ children }) {
  const [currentUser, setCurrentUser] = useState(null);
  const [loading, setLoading] = useState(true);

  // 회원가입
  async function signup(email, password, displayName) {
    const result = await createUserWithEmailAndPassword(auth, email, password);
    await updateProfile(result.user, { displayName });
    return result;
  }

  // 로그인
  function login(email, password) {
    return signInWithEmailAndPassword(auth, email, password);
  }

  // Google 로그인
  async function signInWithGoogle() {
    const provider = new GoogleAuthProvider();
    return signInWithPopup(auth, provider);
  }

  // 로그아웃
  function logout() {
    return signOut(auth);
  }

  // 비밀번호 재설정
  function resetPassword(email) {
    return sendPasswordResetEmail(auth, email);
  }

  useEffect(() => {
    const unsubscribe = onAuthStateChanged(auth, (user) => {
      setCurrentUser(user);
      setLoading(false);
    });

    return unsubscribe;
  }, []);

  const value = {
    currentUser,
    signup,
    login,
    logout,
    signInWithGoogle,
    resetPassword
  };

  return (
    <AuthContext.Provider value={value}>
      {!loading && children}
    </AuthContext.Provider>
  );
}
```

**평가 기준**:
- 기능 완성도: 25점
- 에러 처리: 10점
- UX 고려: 10점

#### 과제 4-2: Firestore 데이터베이스 설계 및 구현 💾
**마감**: 4주차 목요일 오후 8시

**데이터 모델 설계**:
```
collections/
├── users/
│   └── {userId}/
│       ├── profile (document)
│       ├── settings (document)
│       └── activities/ (subcollection)
│           └── {activityId}
├── [yourMainCollection]/
│   └── {documentId}/
│       ├── createdBy: userId
│       ├── createdAt: timestamp
│       ├── updatedAt: timestamp
│       └── [otherFields]
└── [relatedCollection]/
    └── {documentId}
```

**CRUD 작업 구현**:
```jsx
// hooks/useFirestore.js
import { 
  collection, 
  doc, 
  addDoc, 
  getDoc, 
  getDocs, 
  updateDoc, 
  deleteDoc, 
  query, 
  where, 
  orderBy, 
  limit,
  onSnapshot,
  serverTimestamp
} from 'firebase/firestore';
import { db } from '../firebase';

export function useFirestore(collectionName) {
  // Create
  async function addDocument(data) {
    try {
      const docRef = await addDoc(collection(db, collectionName), {
        ...data,
        createdAt: serverTimestamp(),
        updatedAt: serverTimestamp()
      });
      return docRef.id;
    } catch (error) {
      throw new Error(`Failed to add document: ${error.message}`);
    }
  }

  // Read
  async function getDocument(id) {
    try {
      const docRef = doc(db, collectionName, id);
      const docSnap = await getDoc(docRef);
      
      if (docSnap.exists()) {
        return { id: docSnap.id, ...docSnap.data() };
      } else {
        return null;
      }
    } catch (error) {
      throw new Error(`Failed to get document: ${error.message}`);
    }
  }

  // Update
  async function updateDocument(id, data) {
    try {
      const docRef = doc(db, collectionName, id);
      await updateDoc(docRef, {
        ...data,
        updatedAt: serverTimestamp()
      });
    } catch (error) {
      throw new Error(`Failed to update document: ${error.message}`);
    }
  }

  // Delete
  async function deleteDocument(id) {
    try {
      await deleteDoc(doc(db, collectionName, id));
    } catch (error) {
      throw new Error(`Failed to delete document: ${error.message}`);
    }
  }

  // Real-time listener
  function useRealtimeCollection(queryOptions = {}) {
    const [documents, setDocuments] = useState([]);
    const [loading, setLoading] = useState(true);
    const [error, setError] = useState(null);

    useEffect(() => {
      let q = collection(db, collectionName);
      
      if (queryOptions.where) {
        q = query(q, where(...queryOptions.where));
      }
      
      if (queryOptions.orderBy) {
        q = query(q, orderBy(...queryOptions.orderBy));
      }
      
      if (queryOptions.limit) {
        q = query(q, limit(queryOptions.limit));
      }

      const unsubscribe = onSnapshot(q, 
        (querySnapshot) => {
          const docs = querySnapshot.docs.map(doc => ({
            id: doc.id,
            ...doc.data()
          }));
          setDocuments(docs);
          setLoading(false);
        },
        (error) => {
          setError(error.message);
          setLoading(false);
        }
      );

      return () => unsubscribe();
    }, [JSON.stringify(queryOptions)]);

    return { documents, loading, error };
  }

  return {
    addDocument,
    getDocument,
    updateDocument,
    deleteDocument,
    useRealtimeCollection
  };
}
```

**평가 기준**:
- 데이터 모델 설계: 20점
- CRUD 구현: 20점
- 실시간 기능: 10점

#### 과제 4-3: 전체 기능 통합 🔗
**마감**: 4주차 금요일 오후 8시

**통합 요구사항**:
- [ ] 로그인한 사용자만 핵심 기능 접근 가능
- [ ] 사용자별 데이터 분리
- [ ] 실시간 데이터 업데이트
- [ ] 로딩/에러 상태 UI
- [ ] 오프라인 지원 (선택)

### 선택 과제

#### 보너스 과제 4-1: Firebase Security Rules 작성 ✨
**보너스 점수**: +10점

**요구사항**:
```javascript
// firestore.rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users can only access their own user document
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // [Your collection] - users can only access their own documents
    match /[yourCollection]/{docId} {
      allow read, write: if request.auth != null && 
        request.auth.uid == resource.data.createdBy;
    }
  }
}
```

---

## 🎯 5주차 과제: 배포 & 자동화

### 필수 과제

#### 과제 5-1: GitHub Actions CI/CD 구축 ⚙️
**마감**: 5주차 화요일 오후 8시

**워크플로우 파일**:
```yaml
# .github/workflows/deploy.yml
name: Deploy to Vercel

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run linting
      run: npm run lint
    
    - name: Run tests
      run: npm run test
    
    - name: Build project
      run: npm run build
      env:
        VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
        VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
        VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Deploy to Vercel
      uses: amondnet/vercel-action@v25
      with:
        vercel-token: ${{ secrets.VERCEL_TOKEN }}
        vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
        vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
        vercel-args: '--prod'
```

**평가 기준**:
- CI/CD 설정 완료: 20점
- 자동 테스트 통과: 10점
- 자동 배포 성공: 15점

#### 과제 5-2: 환경별 배포 설정 🌍
**마감**: 5주차 목요일 오후 6시

**환경 구성**:
- **Development**: feature/* 브랜치
- **Staging**: develop 브랜치  
- **Production**: main 브랜치

**환경변수 관리**:
```bash
# 환경별 환경변수 설정
vercel env add VITE_FIREBASE_API_KEY development
vercel env add VITE_FIREBASE_API_KEY preview
vercel env add VITE_FIREBASE_API_KEY production

# 각 환경별 다른 Firebase 프로젝트 사용
# dev-project, staging-project, prod-project
```

#### 과제 5-3: 모니터링 및 분석 도구 연동 📊
**마감**: 5주차 금요일 오후 8시

**연동 도구**:
- [ ] Vercel Analytics
- [ ] Google Analytics 4
- [ ] Sentry (에러 추적)
- [ ] Firebase Performance Monitoring

**구현 예시**:
```jsx
// utils/analytics.js
export function trackEvent(eventName, parameters = {}) {
  // Google Analytics
  if (typeof window !== 'undefined' && window.gtag) {
    window.gtag('event', eventName, parameters);
  }
  
  // Vercel Analytics
  if (typeof window !== 'undefined' && window.va) {
    window.va('track', eventName, parameters);
  }
}

// 사용 예시
trackEvent('button_click', {
  button_name: 'signup',
  page: 'landing'
});
```

---

## 🎯 6주차 과제: 고도화 & 최적화

### 필수 과제

#### 과제 6-1: 성능 최적화 ⚡
**마감**: 6주차 수요일 오후 8시

**최적화 항목**:
- [ ] 코드 분할 (React.lazy, 동적 import)
- [ ] 이미지 최적화 (WebP, 지연 로딩)
- [ ] 번들 크기 최적화
- [ ] 캐싱 전략 구현
- [ ] SEO 최적화

**성능 목표**:
- Lighthouse Performance: 90점 이상
- First Contentful Paint: 2초 이내
- Largest Contentful Paint: 3초 이내
- 번들 크기: 500KB 이하

#### 과제 6-2: 사용자 피드백 수집 및 반영 👥
**마감**: 6주차 금요일 오후 8시

**피드백 수집 방법**:
- [ ] 베타 테스터 5명 이상 모집
- [ ] 사용자 인터뷰 진행
- [ ] 분석 도구를 통한 사용 패턴 분석
- [ ] 버그 리포트 수집

**개선 사항 구현**:
- 수집된 피드백 중 중요도 높은 3개 항목 개선
- 개선 전후 비교 데이터 제시

---

## 🎯 7주차 과제: 비즈니스화

### 필수 과제

#### 과제 7-1: 수익화 모델 설계 💰
**마감**: 7주차 수요일 오후 8시

**비즈니스 모델 캔버스 작성**:
```markdown
# 비즈니스 모델 캔버스

## 핵심 파트너
- 

## 핵심 활동
- 

## 핵심 리소스
- 

## 가치 제안
- 

## 고객 관계
- 

## 채널
- 

## 고객 세그먼트
- 

## 비용 구조
- 

## 수익원
- 
```

#### 과제 7-2: 마케팅 전략 및 실행 📈
**마감**: 7주차 금요일 오후 8시

**마케팅 계획**:
- [ ] 타겟 오디언스 정의
- [ ] 채널별 마케팅 전략
- [ ] 콘텐츠 마케팅 계획
- [ ] 소셜미디어 전략
- [ ] 성과 측정 KPI

---

## 🎯 8주차 과제: 발표 & 피드백

### Demo Day 준비

#### 발표 자료 준비 🎤
**마감**: 8주차 목요일 오후 6시

**발표 구성** (10분):
1. **문제 정의** (2분)
2. **솔루션 시연** (5분)
3. **기술적 성과** (2분)
4. **비즈니스 가능성** (1분)

**준비물**:
- [ ] 발표 자료 (PPT/Keynote)
- [ ] 라이브 데모 준비
- [ ] 백업 데모 영상
- [ ] 질의응답 준비

#### 최종 제출물 📋
**마감**: 8주차 금요일 오후 8시

**제출 항목**:
- [ ] 완성된 MVP (배포 URL)
- [ ] GitHub 저장소
- [ ] 프로젝트 문서 (README)
- [ ] 기술 스택 및 아키텍처 설명
- [ ] 사용자 가이드
- [ ] 개발 회고록
- [ ] 향후 개선 계획

---

## 📊 종합 평가 기준

### 점수 배분
| 구분 | 배점 | 설명 |
|------|------|------|
| **주차별 과제** | 400점 | 8주 × 50점 |
| **개인 MVP** | 300점 | 완성도, 창의성, 기술적 구현 |
| **Demo Day 발표** | 200점 | 발표 품질, 시연, Q&A |
| **참여도** | 100점 | 출석, 토론 참여, 동료 리뷰 |
| **보너스** | 50점 | 추가 과제, 기여도 |

### 수료 기준
- **총점 800점 이상** (80%)
- **주차별 과제 80% 이상 제출**
- **MVP 배포 완료**
- **최종 발표 참여**

### 우수 수료자 선발
- **1등**: 950점 이상 → 상금 50만원
- **2등**: 900점 이상 → 상금 30만원  
- **3등**: 850점 이상 → 상금 10만원

---

**다음**: 🧱 Notion 템플릿 허브 생성